server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: gateway
          uri: https://github.com/Dimas-Peterson/curso-microservicio-2
          search-paths: config-data
  application:
    name: config-service